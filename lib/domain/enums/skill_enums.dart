/// High-level speaking skill branches on the Skill Tree.
enum SkillCategory {
  fluency,
  pronunciation,
  vocabulary,
  grammar,
  storytelling,
  argumentation,
  listening,
  confidence,
}

/// Unlock / mastery state of a skill node.
enum SkillStatus {
  locked,
  available,
  inProgress,
  mastered,
}
